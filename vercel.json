{
  "build": {
    "env": {},
    "builder": "npm install --legacy-peer-deps && npx prisma generate && npm run build",
    "use": "@vercel/next"
  },
  "framework": {
    "name": "nextjs",
    "config": {
      "experimental": {
        "turbo": false
      }
    }
  }
}
